import {Box} from "@mui/material"

const Mobile = () => { 
    return(
        <Box sx={{display:"flex", flexDirection: 'column', justifyContent: 'center', alignItems: 'center', alignContent: 'center', height:"85vh"}}>
            Hello Mobile!
        </Box>
        )
}

export default Mobile;